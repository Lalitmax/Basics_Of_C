#include<stdio.h>

int main()

{
    int a,b;
    a=0;
    b=2;
    printf("a &&b = %d\n",    !(a && b) );   /*logical but operator it is used to reverse the logical state of its
                                                                            operands if condition is true. the logical  not operator will make is false
                                                                            */

    return  0;
}
