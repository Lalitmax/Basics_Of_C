#include<stdio.h>
 int main()
 {
 int a,b;
 a=23;
 b=0;
 printf("a || b = %d\n ", a|| b );  // logical operator if any of these operands non- zero . then condition is become true  = ||
  return  0;
 }
