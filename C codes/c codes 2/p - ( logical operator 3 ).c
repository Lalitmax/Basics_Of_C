#include<stdio.h>
 int main()
 {
 int a,b;
 a=23;
 b=0;
 printf(" !(a && b) = %d\n", !(a && b) );    // logical but . it is used to reverse the logical statr of its operands -
                                                           // icondition is true . the logical not operands will make is folse

 return  0;
 }
