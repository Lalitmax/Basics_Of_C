#include<stdio.h>

int main()

{
    int a,b;
    a=0;
    b=0;
    printf("a &&b = %d\n", a || b);  // logical or operator if  any of  these two operands is non zero then condition will true
    return  0;
}
