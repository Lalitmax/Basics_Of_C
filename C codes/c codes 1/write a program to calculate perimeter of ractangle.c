#include<stdio.h>
int main()
{
    int length,width, x;
    printf("enter length and width of ractangle\n");
    scanf("%d %d", &length,&width);
    x=length +width;
    printf("%d", x*2) ;
    return 0;
}
